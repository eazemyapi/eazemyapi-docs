# Custom Queries

For advanced use cases, EazeMyAPI allows developers to write custom database queries.

This provides flexibility beyond the automatically generated APIs.

Examples include:

* complex filters
* joins between tables
* analytics queries
* custom reports

Custom queries allow developers to combine **no-code simplicity with developer flexibility**.
